export interface IAnalyticsTabs {
  convertedId: number;
}

export enum TabType {
  wallet = 'wallet',
  inflow = 'inflow',
  history = 'orders-history',
  trades = 'trades',
  alerts = 'alerts',
  orders = 'orders',
}
